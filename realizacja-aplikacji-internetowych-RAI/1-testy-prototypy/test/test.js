var expect = require('chai').expect;
const Vehicle = require('../src/index').Vehicle;

describe('canary-tests', function () {
  it('should update max velocity', function () {
    const car = new Vehicle(0);
  });
});
